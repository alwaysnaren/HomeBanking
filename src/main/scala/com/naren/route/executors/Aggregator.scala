package com.naren.route.executors

import com.naren.route.data.Reader
import com.naren.route.dataType.Loan

import scala.collection.mutable.ArrayBuffer
import scala.reflect.ClassTag

object Aggregator {

//  def getAllRecords[N <: Record[N]: ClassTag](name: String, key: String): Option[Seq[N]] = {
//    var yb = Reader.yb
//    val buff = ArrayBuffer[N]()
//    while(yb.exists) {
//      val page = yb.getPage[N](name)
//      if(page != null) {
//        import reflect.classTag
//        val tag = ClassTag[N]
//        val recs =
//          tag match {
//            case t if t == classTag[Loan] =>
//          }
//      }
//    }
//  }

}
